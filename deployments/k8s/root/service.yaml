apiVersion: v1
kind: Service
metadata:
  name: root-service
  labels:
    app.kubernetes.io/part-of: kube-itog-tsitanya
spec:
  selector:
    app: root-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
